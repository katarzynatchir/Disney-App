import React from 'react';
import styled from 'styled-components';

const SwitcherStyled = styled.div`
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 0.8rem;
`;

const Switcher = () => {
  return <SwitcherStyled>Switcher</SwitcherStyled>;
};

export default Switcher;
